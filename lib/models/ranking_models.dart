class RankingModel{
  String? title; // can be bowler name, country name
  String? country;
  String? rating;
  String? profileUrl;
  String? dpUrl;
  String? points;
  String? changeInRanks;
  RankingModel({this.title, this.country, this.rating, this.points, this.dpUrl, this.profileUrl, this.changeInRanks});
}